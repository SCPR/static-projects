<!-- begin -->
<div class="congressional-container">

    {{#objects}}
        <!-- begin row -->
        <div class="row-fluid">

            <!-- begin main span -->
            <div class="span12">
                <div id="{{speaker_first}}_{{speaker_last}}_{{number}}" class="row-fluid" style="padding: 25px 0 15px 0;">
                    <div class="span2">
                        <dl>
                            <dd>
                                <div class="img">
                                    <img src="{{speaker_image_url}}" alt="{{speaker_first}}_{{speaker_last}}" width="120px" height="170px" />
                                </div>
                                <div class="text">
                                    <p class="data-instructions"><strong>{{speaker_first}} {{speaker_last}}</strong></p>
                                </div>
                            </dd>
                        </dl>
                    </div>
                    <div class="span10">
                        <dl>
                            <dd>
                                <h4>{{speaker_reference}}</h4>
                            </dd>
                            <dd>
                                <p class="data-instructions">From remarks titled <strong>{{title}}</strong>, spoken on {{dateFormat date}} in the {{chamber}}.</p>
                                <div class="speech-text">
                                    {{#each speaking}}
                                        <p>{{this}}</p>
                                    {{/each}}
                                </div>
                                <p><a href="{{origin_url}}" target="_blank"><strong>Read Source</strong></a></p>
                            </dd>
                        </dl>
                    </div>
                </div>
            </div>
            <!-- end main span -->

        </div>
        <!-- end row -->

        <hr>
    {{/objects}}

</div>
<!-- end -->